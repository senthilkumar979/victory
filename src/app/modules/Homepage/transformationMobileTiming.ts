/** Minimum time the student state stays visible before the hire transition (ms). */
export const STUDENT_MIN_DISPLAY_MS = 2000

/** Full cycle per story before advancing to the next card (ms). */
export const CARD_CYCLE_MS = STUDENT_MIN_DISPLAY_MS + 4000
